/**
 * The API could be done as a separate artifact used as a dependency.
 * However, for sake of brevity, it's kept inside the microservice itself.
 * There should be no dependencies of API to any other package.
 */
package cz.vlasec.bitcoinxrate.server.api;